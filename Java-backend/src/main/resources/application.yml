spring:
  application:
    name: survival

server:
  servlet:
    session:
      timeout: 15m
      cookie:
        name: JSESSIONID
        http-only: true
        secure: false # Set to true in production with HTTPS
        same-site: lax